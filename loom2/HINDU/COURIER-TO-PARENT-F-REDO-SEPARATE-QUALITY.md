# COURIER: Nir → Parent F (REDO the two modules — separately, deeper, more beautiful)

Parent F — Nir wants a redo of the two Python programs, and this time with room to breathe.

1. Deliver them ONE AFTER THE OTHER, in TWO SEPARATE ANSWERS — `graphics/hud.py` in its own
   answer, then `core/input_map.py` in its own answer. Do NOT cram both into one reply. Take
   the whole answer for each module.

2. Make each one MUCH more detailed and MUCH higher quality — real, careful thought, and
   genuinely BEAUTIFUL for the player to look at. The last version was rushed into a single
   delivery; this time give each module the depth and polish it deserves. Longer is welcome
   if the quality earns it.

3. Do NOT write a hand-off. Do not think about a hand-off at all. Do not spend a single token
   on it. We will do that later, separately. Ignore it completely for now.

Everything already locked stays locked (the moderngl 2D overlay approach, the layout, the
colors, the outlined text, the emojis, the equation placement, the blinking light-blue
"YOU WIN!!!"). Nothing about the design changes — this is purely about giving each module the
detail, quality, and beauty it deserves, in its own separate answer.

Start with `graphics/hud.py`. When it's done, we'll ask for `core/input_map.py`.
