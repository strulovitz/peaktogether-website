__pycache__/
*.pyc
*.log

# Build artifacts (PyInstaller, build venvs) — never commit these
build/
dist/
release/
.venv-build/

# LOOM generated assets (never commit these)
loom/fixtures/audio_beeps/
loom/forge/forged/
