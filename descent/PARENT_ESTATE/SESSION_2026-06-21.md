# SESSION CONTEXT — June 21, 2026

> **Project:** DESCENT QED engine + Peak Together website
> **Repo:** `C:\Users\nir_s\peaktogether-website`
> **GitHub:** `https://github.com/strulovitz/peaktogether-website`
> **Builder:** DeepSeek V4 Pro (OpenCode)
> **Time:** Evening, Israel time. Nir is going to sleep.

---

## WHAT HAPPENED TODAY — HUGE DAY

### 1. Mission A — Repo Reorganization COMPLETE
ALL Descent QED files (50 items) moved into `descent/` folder via `git mv`. Zero code changes needed — imports and relative paths work unchanged when running from `descent/`. Website files stay in root. Future games get their own top-level folders.

Game launch command: `cd C:\Users\nir_s\peaktogether-website\descent && python app.py`

### 2. Mission B — Per-Corridor Baked Image Isolation COMPLETE
Each corridor now has its own isolated baked-image subfolder. Manifest format updated: each corridor line has `baked=<path>` annotation. `level_parser.py` updated to parse per-corridor baked paths with global fallback. Backward compatible with old manifests.

Folder convention: `descent/baked/<subject>/<corridor_slug>/robotN_layer.png`

### 3. FOREVER Prompt — MAJOR OVERHAUL (13+ fixes from full audit)
The corridor-creator prompt was thoroughly audited against the actual baker/parser code. Fixes applied:

- **CRITICAL: Stain `$...$` rule** — `\stain{}{}` expands to `\colorbox{}` (text mode), so math inside needs its own `$...$`. Added CORRECT/WRONG examples.
- **CRITICAL: Thread double-dollar trap** — `$\thread{t1}{$x$}$` creates 4 dollar signs and crashes. Added explicit warning with 5 CORRECT/WRONG examples.
- **CRITICAL: Bake command** — was missing required `--out` flag. Fixed with example.
- **CRITICAL: Manifest template** — was showing old global `baked:` format. Fixed to per-corridor `baked=` format.
- **CRITICAL: LEDGER template** — had bare `PRIMARY = yellow` without key name. Fixed.
- **IMPORTANT: Engineer layer** — added full value-arc system (`[[ $expr$ | value ]]`), design intent (pilot's hero move, boyfriend saves the day), based on physicist layer (most math), arcs on EVERY expression.
- **IMPORTANT: Physicist layer** — must elaborate intermediate steps, open compact expressions (e.g. $(a+b)^2$ → $a^2+2ab+b^2$), contains the MOST math.
- **IMPORTANT: `\text{}` added** to allowed baker commands.
- **IMPORTANT: Baker NAME vs game NAME** — can differ (technique vs person).
- Plus: SEGMENTS notation, portrait location, VULNERABLE_TO comment, PROBLEM example fixes.

FOREVER prompt link: https://github.com/strulovitz/peaktogether-website/blob/master/descent/PARENT_ESTATE/CORRIDOR_CREATOR_PROMPT_FOREVER.md

### 4. Basel Corridor 2 — TWO child attempts

**First child (OLD, saved with OLD_ prefix):**
- Engineer layer had NO value arcs (prompt didn't mention them yet)
- Files renamed to: `corridors/OLD_basel_general.txt`, `levels/.../OLD_basel_general.txt`, `baked/basel/OLD_euler_generalizations/`
- Baker file also had stain `$...$` errors (10 failures)

**Second child (CURRENT, working):**
- Topic: "Euler's Symmetric-Polynomial Ascent"
- 7 robots: Euler, Newton, Girard, Waring, Bernoulli, Lindemann, Apery
- Engineer layers have proper value arcs
- Baker file had thread double-dollar errors (8 failures) — fixed mechanically by DeepSeek (regex removed 38 inner `$` from threads)
- **28/28 baked images — 0 failures after fix**
- Files: `corridors/basel_symmetric.txt`, `levels/.../basel_symmetric_proof.txt`, `baked/basel/symmetric_polys/`

### 5. New Portraits Added (7 total this session)
- Jacob_Bernoulli, Isaac_Newton, Albert_Girard, Pietro_Mengoli (from first child attempt)
- Edward_Waring, Ferdinand_von_Lindemann, Roger_Apery (from second child)

---

## CURRENT STATE OF THE REPO

| Item | Status |
|------|--------|
| Repo reorganization (descent/ folder) | COMPLETE |
| Per-corridor baked image isolation | COMPLETE |
| FOREVER prompt (all fixes) | COMPLETE |
| Basel corridor 1 (Euler's 1734 approach) | PLAYABLE, baked, tested |
| Basel corridor 2 (Symmetric-Polynomial Ascent) | PLAYABLE, baked, tested, Nir confirmed "it works great" |
| app.py points to | `levels/basel_c2.txt` (corridor 2 for testing) |
| Git | Clean, pushed |

---

## WHAT TO DO TOMORROW — IN ORDER

1. **Continue making Basel corridors** — Nir wants ~10 corridors total for the Basel Problem. We have 2 done. Each new corridor:
   - Paste FOREVER prompt to a fresh child Opus
   - Child produces 3 files (baker + game + manifest)
   - DeepSeek saves files, bakes, fixes any LaTeX errors, tests
   - If thread double-dollar issue recurs, fix mechanically with regex

2. **Potential topics for remaining Basel corridors** (from Wikipedia's Basel problem article — Nir will choose):
   - Euler's original 1735 proof (the sine product)
   - Cauchy's proof using Fourier series
   - Proof using the residue theorem
   - Proof using Euler-Maclaurin summation
   - Proof using double integrals
   - Proof using the Weierstrass product
   - Connection to the Riemann zeta function
   - Historical context and Mengoli's challenge
   - And more approaches

3. **After all Basel corridors are done:**
   - Test multi-corridor play (both corridors in one level)
   - Move on to a NEW subject (Riemann Hypothesis? Navier-Stokes? Nir decides)

---

## HOW TO LAUNCH THE GAME

```
cd C:\Users\nir_s\peaktogether-website\descent
python app.py
```

Controls: WASD/RF move, arrows rotate, Q/E roll, Shift boost, `[`/`]` cycle weapon, SPACE fire, U = Understanding Mode near robot, ESC = quit.

Currently loads corridor 2 (Symmetric-Polynomial Ascent). To switch back to corridor 1, change `LEVEL_MANIFEST` in app.py to `"levels/basel.txt"` and it loads corridor 1 only (or both corridors once multi-corridor is ready).

---

## ON RESTART — Read these in order:
1. **descent/WORKFLOW.md** (project memory)
2. **This file** (`SESSION_2026-06-21.md`)
3. `descent/PARENT_ESTATE/PARENT_HANDOFF_V3.md` — THE LAW

---

Good night Nir! Sleep well! Tomorrow we build more corridors! :-)
